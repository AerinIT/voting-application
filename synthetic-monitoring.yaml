apiVersion: 1

apps:
  - type: grafana-synthetic-monitoring-app
    name: grafana-synthetic-monitoring-app
    disabled: false
    jsonData:
      apiHost: 
      stackId:
      logs:
        grafanaName:  
        hostedId:
      metrics:
        grafanaName:
        hostedId:
    secureJsonData:
      publisherToken: 

